--- os/linux/Makefile.6
+++ os/linux/Makefile.6
@@ -721,8 +721,8 @@
 	rm -rf $(DAT_PATH)
 	mkdir -pv $(DAT_PATH)
 	cp $(RT28xx_DIR)/$(DAT_FILE_NAME) $(DAT_PATH)/.
-	install -d $(LINUX_SRC_MODULE)
-	install -m 644 -c $(addsuffix .ko,$(MOD_NAME)) $(LINUX_SRC_MODULE)
+	install -d $(DESTDIR)/usr$(LINUX_SRC_MODULE)
+	install -m 644 -c $(addsuffix .ko,$(MOD_NAME)) $(DESTDIR)/usr$(LINUX_SRC_MODULE)
 	/sbin/depmod -a ${shell uname -r}
 
 uninstall:
